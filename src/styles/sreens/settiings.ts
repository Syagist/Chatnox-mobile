import {StyleSheet} from "react-native";
import {COLOR_DARK_BG, COLOR_WHITE} from "@/constants/Colors";

export const settiings = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: COLOR_DARK_BG,
        position: 'relative'
    },
});